<% if resource.errors.any? %>
  <div id="error_explanation">
    <h5>The form containes <%= pluralize(resource.errors.count, "error") %>.
    </h5>
    <ul>
      <% resource.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
